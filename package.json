{
  "name": "theme-atlas",
  "version": "1.0.0",
  "private": true,
  "description": "A static theme viewer and reusable color-token catalog.",
  "type": "module",
  "scripts": {
    "build": "node tools/build-themes.mjs",
    "validate": "node tools/validate.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "npm run build && npm run validate && npm test",
    "dev": "node --watch tools/build-themes.mjs"
  }
}
